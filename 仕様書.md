# AccountingApp 仕様書

## 1. 文書の目的
本書は `AccountingApp` の機能仕様、業務ルール、データ構成、運用上の前提を整理したものです。実装基準日は 2026-05-05 時点のリポジトリ内容です。

## 2. システム概要
- 種別: Windows デスクトップ会計アプリケーション
- UI: Avalonia
- データベース: SQLite
- 利用単位: 1つの DB ファイルにつき 1社
- 認証方式: アプリ内ユーザー認証
- 主な対象業務:
  - 会社初期設定
  - ユーザー管理
  - 勘定科目、補助科目、取引先管理
  - 仕訳入力
  - 仕訳帳、出納帳、総勘定元帳
  - 取引先別取引一覧
  - 試算表、貸借対照表、損益計算書
  - 年度締め、繰越仕訳作成

## 3. 起動と初期化
### 3.1 起動シーケンス
1. 起動時に DB 選択画面を表示する。
2. 前回設定済み DB ファイルが存在する場合は自動で開く。
3. DB が未設定、または前回の DB が見つからない場合は以下を選択できる。
   - 既存の SQLite DB を開く
   - 新しい SQLite DB を作成する

### 3.2 DB 新規作成
- 新規 DB 作成時は `Database/schema.sql` を実行してスキーマを初期化する。
- DB 作成後、ログイン画面から初期管理者を作成できる。

### 3.3 初期管理者作成
- 初期管理者作成時に以下を登録する。
  - 会社名
  - 会計年度開始日
  - 締め日
  - 免税事業者フラグ
  - 初期勘定科目セット
  - 管理者ログイン ID、表示名、パスワード
- パスワードは 8 文字以上必須。
- 初期勘定科目は以下のいずれかで投入する。
  - プリセット
  - `Database/seed_accounts.csv` と同等列構成の CSV
- 初期作成時に既定税区分を投入する。
- 主科目ごとに補助科目コード `0` の既定補助科目を自動作成する。

## 4. 利用者と権限
### 4.1 ユーザーロール
- `admin`
  - すべての管理機能を利用可能
  - 会社設定、勘定科目、補助科目、取引先、ユーザー管理を利用可能
  - ALL CLEAR を実行可能
- `staff`
  - 日常業務画面と帳票画面を利用可能
  - 管理画面は利用不可

### 4.2 認証
- 有効なユーザーのみログインできる。
- ログイン時は `users.is_active = true` を満たすこと。
- ログイン後は、そのユーザーに紐づく会社とロールを読み込む。
- SQLite 版は 1DB = 1社を前提とし、複数会社が同居している場合はエラーとする。

## 5. 画面一覧
### 5.1 起動画面
- DB 選択画面
- ログイン画面
- 初期管理者作成画面

### 5.2 ホーム
- サマリー表示
  - 勘定科目数
  - 補助科目数
  - 仕訳件数
  - 仕訳金額
- メニュー区分
  - 日常業務
  - 帳票
  - 管理

### 5.3 管理機能
- 会社設定
- 勘定科目登録
- 補助科目登録
- 取引先マスタ
- ユーザー管理

### 5.4 日常業務・帳票
- 振替伝票
- 仕訳帳
- 出納帳
- 総勘定元帳
- 取引先別取引一覧
- 試算表
- 貸借対照表
- 損益計算書

## 6. 機能仕様
### 6.1 会社設定
- 管理項目:
  - 会社名
  - 会計年度開始日
  - 締め日
  - 消費税記帳方式
  - 免税事業者フラグ
- 消費税記帳方式は `gross` または `net`。
- 免税事業者の場合、記帳方式は内部的に `gross` に正規化される。

### 6.2 ユーザー管理
- 登録項目:
  - ログイン ID
  - 表示名
  - パスワード
  - ロール
  - 有効フラグ
- 一般ユーザーは他ユーザーのパスワード変更不可。
- パスワード変更時は現在のパスワード確認が必要。

### 6.3 勘定科目管理
- 登録項目:
  - 勘定科目コード
  - 勘定科目名
  - 勘定科目区分
  - 残高性質
  - 既定税区分
  - 補助科目ありフラグ
  - 有効フラグ
- CSV 取込、CSV 保存に対応。
- 勘定科目区分:
  - `asset`
  - `liability`
  - `equity`
  - `revenue`
  - `expense`
- 残高性質:
  - `debit`
  - `credit`

### 6.4 補助科目管理
- 登録項目:
  - 主科目
  - 補助コード
  - 補助科目名
  - 外部コード
  - 開始残高
  - 有効フラグ
- CSV 取込、CSV 保存に対応。
- 仕訳入力時に補助科目未指定で登録する場合は `sub_account_id = 0` を採用する。

### 6.5 取引先管理
- 登録項目:
  - 取引先コード
  - 取引先名
  - 取引先区分
  - インボイス区分
  - 登録番号
  - 有効フラグ
- 取引先区分:
  - `customer`
  - `supplier`
  - `both`
  - `other`
- インボイス区分:
  - `qualified`
  - `exempt`
  - `unregistered`
  - `unknown`

### 6.6 仕訳入力
- 伝票ヘッダ項目:
  - 取引日
  - 伝票番号
  - 証憑番号
- 明細行数:
  - 最大 10 行
- 明細項目:
  - 借方コード、借方科目
  - 貸方コード、貸方科目
  - 借方税区分
  - 借方税入力方式
  - 借方金額
  - 貸方税区分
  - 貸方税入力方式
  - 貸方金額
  - 摘要
  - 取引先
  - 請求書番号

#### 6.6.1 登録方式
- 通常モード
  - 1つの伝票に複数行を登録する。
  - 借方合計と貸方合計の一致が必要。
- 単仕訳モード
  - 各行を別伝票として登録する。
  - 借方金額入力時に貸方金額へ同額を自動反映する。
  - 既存伝票編集時は使用不可。

#### 6.6.2 仕訳入力バリデーション
- 金額は 0 より大きい数値必須。
- 科目未選択で金額だけ入力された場合はエラー。
- 通常モードでは借方合計と貸方合計が一致しない場合は登録不可。
- 補助科目未指定時は確認ダイアログを表示し、同意時に `0` で登録する。
- 締め済み年度に属する仕訳は新規登録、更新、削除不可。

### 6.7 仕訳帳
- 月単位で表示する。
- 前月、次月、当月切替に対応。
- 機能:
  - 新規仕訳
  - 既存仕訳編集
  - 仕訳削除
  - CSV 取込
  - CSV 出力
  - PDF 出力

### 6.8 出納帳
- 勘定科目、補助科目、対象月で絞り込み表示する。
- 表示内容:
  - 前月残高
  - 入金合計
  - 出金合計
  - 残高

### 6.9 総勘定元帳
- 勘定科目、補助科目、対象月で絞り込み表示する。
- 表示内容:
  - 繰越残高
  - 借方合計
  - 貸方合計
  - 残高
- PDF 出力に対応。

### 6.10 取引先別取引一覧
- 取引先、開始日、終了日で絞り込み表示する。
- 借方合計、貸方合計を集計表示する。

### 6.11 試算表
- 開始日、終了日を指定して表示する。
- 勘定科目分類ごとに集計する。
- 分類は `Models/AccountClassificationCatalog.cs` のコード帯で判定する。

### 6.12 貸借対照表
- 指定日時点の残高を表示する。
- 集計表示:
  - 資産合計
  - 負債合計
  - 資本合計
  - 当期純損益
  - 負債・資本合計
- PDF 出力に対応。

### 6.13 損益計算書
- 期間指定で表示する。
- 集計表示:
  - 売上高
  - 売上原価
  - 売上総利益
  - 営業利益
  - 販管費
  - 営業外等収益
  - 営業外等費用
  - 当期純損益
- PDF 出力に対応。

### 6.14 年度締め
- 対象年度を締め、翌年度開始日に繰越仕訳を作成または再作成する。
- 年度締め実行後:
  - 対象年度は `closed`
  - 対象年度の仕訳追加、更新、削除を禁止
- 締め解除時:
  - 理由入力必須
  - ステータスを `open` に戻す
  - 修正後に再締めが必要

#### 6.14.1 年度繰越の概要
- 損益残高を集計し、翌年度開始日に繰越伝票を作成する。
- 差額の相手科目は資本科目から選定する。
- 作成済みの同一繰越仕訳がある場合は削除後に再作成する。

### 6.15 ALL CLEAR
- 管理者のみ実行可能。
- `CLEAR` の確認入力が必要。
- 実行対象:
  - ユーザー
  - 勘定科目
  - 補助科目
  - 取引先
  - 仕訳
- 実行後はログイン画面へ戻る想定。

## 7. 消費税仕様
消費税の詳細は [Database/TAX_SPEC.md](Database/TAX_SPEC.md) を参照とし、本書では要点のみを記載する。

### 7.1 会社設定との関係
- `gross`
  - 入力候補は `none` と `included`
- `net`
  - 入力候補は `none` と `excluded`
- 免税事業者
  - すべて `none`
  - 税額、控除可税額、控除不可税額は 0

### 7.2 既定税区分
初期投入される代表税区分:
- `SALES10`
- `SALES8`
- `PURCHASE10`
- `PURCHASE8`
- `PURCHASE10_NC`
- `PURCHASE8_NC`
- `NONTAX`
- `EXEMPT`
- `OUT`

### 7.3 仕入税額控除
- 取引先のインボイス区分と登録番号に応じて控除率を決定する。
- 仕訳行には以下を保持する。
  - 税額
  - 控除可税額
  - 控除不可税額
  - インボイス区分
  - 登録番号
  - 控除率

## 8. CSV 仕様
### 8.1 勘定科目 CSV
ヘッダ:
`code,name,account_type,balance_side,is_control_account,default_tax_code,is_active`

### 8.2 補助科目 CSV
ヘッダ:
`account_code,code,name,external_code,balance,is_active`

### 8.3 仕訳 CSV
ヘッダ:
`entry_date,entry_number,reference,side,account_code,sub_account_code,amount,tax_code,tax_rate,tax_amount,creditable_tax_amount,non_creditable_tax_amount,tax_input_type,partner_code,invoice_number,invoice_registration_number,invoice_status,purchase_credit_rate,description`

### 8.4 CSV 共通仕様
- 取込時はアプリ出力 CSV と同一ヘッダを要求する。
- 文字列中の改行は `\n` でエスケープする。
- 勘定科目 CSV の真偽値は `true/false` または `1/0`。

## 9. PDF 出力仕様
- 対象帳票:
  - 仕訳帳
  - 総勘定元帳
  - 貸借対照表
  - 損益計算書
- 出力後は OS 既定アプリで PDF を自動オープンする。
- 自動起動できない場合でも PDF ファイル自体は保存済みとして扱う。

## 10. データ構成
### 10.1 主要テーブル
- `companies`
- `users`
- `user_companies`
- `accounts`
- `sub_accounts`
- `sub_account_balances`
- `tax_codes`
- `business_partners`
- `journal_vouchers`
- `journal_lines`
- `annual_carry_forwards`
- `annual_closings`
- `operation_logs`

### 10.2 関連資料
- テーブル仕様: [Database/TABLE_SPEC.md](Database/TABLE_SPEC.md)
- 消費税仕様: [Database/TAX_SPEC.md](Database/TAX_SPEC.md)
- ER 図: [Database/ERD.md](Database/ERD.md)

## 11. 制約事項
- SQLite 版は 1DB 1社運用を前提とする。
- 既存伝票編集では単仕訳モードを使用できない。
- 帳票分類は勘定科目コード帯に依存するため、コード設計が帳票表示へ影響する。
- 締め済み年度の仕訳変更は不可。
- CSV 取込は任意フォーマットではなく、アプリ出力フォーマット準拠が前提。

## 12. 今後の保守観点
- 本仕様書は画面文言、CSV 列、税計算、締め処理が変更された際に更新する。
- DB スキーマの詳細変更時は `Database/TABLE_SPEC.md` も合わせて更新する。
