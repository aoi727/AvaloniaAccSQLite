# AccountingApp 操作説明書

## 1. 本書の対象
本書は `AccountingApp` を初めて利用する担当者向けの操作手順書です。初回セットアップ、日常操作、帳票出力、年度締めまでを説明します。

## 2. 起動から利用開始まで
### 2.1 アプリ起動
1. アプリを起動します。
2. 「使用するDBファイルを選択」画面が表示されます。
3. 次のどちらかを選びます。
   - `新しいDBを作成`
   - `DBファイルを選ぶ`

### 2.2 新しい DB を作成する
1. `新しいDBを作成` を押します。
2. 保存先とファイル名を指定します。
3. DB 初期化完了後、ログイン画面へ進みます。

### 2.3 既存 DB を開く
1. `DBファイルを選ぶ` を押します。
2. 対象の `.db` / `.sqlite` / `.sqlite3` ファイルを選択します。
3. ログイン画面へ進みます。

## 3. 初回セットアップ
### 3.1 初期管理者を作成する
DB を新規作成した直後、またはユーザー未登録の DB では、ログイン画面の `初期管理者を作成` を使います。

入力項目:
- 会社名
- 会計年度開始日
- 締め日
- `免税事業者`
- 勘定科目の初期設定
- ログインID
- 表示名
- パスワード
- パスワード確認

勘定科目の初期設定は次のどちらかです。
- `プリセットを使う`
- `CSVファイルから設定する`

CSV を使う場合:
1. `CSVファイルから設定する` を選びます。
2. `勘定科目CSVを選択` を押します。
3. `Database/seed_accounts.csv` と同じ列構成の CSV を選択します。

注意:
- パスワードは 8 文字以上必要です。
- 補助科目コード `0` は自動作成されます。

## 4. ログイン
1. `ログインID` を入力します。
2. `パスワード` を入力します。
3. `ログイン` を押します。

補助操作:
- `DBスキーマを初期化`
  - テーブル未作成 DB を初期化します。
- `初期管理者を作成`
  - ユーザー未登録 DB の初期セットアップに使います。

## 5. ホーム画面
ログイン後はホーム画面が表示されます。

表示内容:
- 会社名
- ログインユーザー名 / ロール
- サマリー
  - 勘定科目
  - 補助科目
  - 仕訳件数
  - 仕訳金額

メニュー:
- 日常業務
  - `仕訳入力`
  - `仕訳帳`
- 帳票
  - `出納帳`
  - `総勘定元帳`
  - `取引先別取引一覧`
  - `試算表`
  - `貸借対照表`
  - `損益計算書`
  - `消費税集計表`
- 管理
  - `会社管理`
  - `勘定科目`
  - `補助科目`
  - `取引先`
  - `ユーザー`
  - `操作履歴`

`staff` ロールでは管理メニューは利用できません。

## 6. マスタ管理
### 6.1 勘定科目を登録する
1. ホーム画面で `勘定科目` を押します。
2. 以下を入力します。
   - 勘定科目コード
   - 勘定科目名
   - 勘定科目区分
   - 残高性質
   - 既定税区分
   - `補助科目あり`
3. `登録する` を押します。

補助操作:
- `新規に戻す`
- `CSV取込`
- `CSV保存`
- `補助科目管理へ`

### 6.2 勘定科目 CSV を取り込む
1. 勘定科目画面で `CSV取込` を押します。
2. アプリ出力形式の CSV を選びます。
3. 取込完了メッセージを確認します。

### 6.3 補助科目を登録する
1. ホーム画面で `補助科目` を押します。
2. 以下を入力します。
   - 主科目
   - 補助コード
   - 補助科目名
   - 外部コード
   - 開始残高
3. `登録する` を押します。

補助操作:
- `新規に戻す`
- `CSV取込`
- `CSV保存`
- `ホームに戻る`

### 6.4 取引先を登録する
1. ホーム画面で `取引先` を押します。
2. 以下を入力します。
   - 取引先コード
   - 取引先名
   - 取引先区分
   - インボイス区分
   - 登録番号
   - `有効`
3. `登録する` を押します。

### 6.5 ユーザーを登録する
1. ホーム画面で `ユーザー` を押します。
2. 以下を入力します。
   - ログインID
   - 表示名
   - 新しいパスワード
   - 新しいパスワード(確認)
   - ロール
   - `有効`
3. `登録する` を押します。

ユーザー編集時の注意:
- 自分のパスワード変更には現在のパスワード入力が必要です。
- 一般ユーザーは他ユーザーのパスワード変更はできません。

## 7. 仕訳入力
### 7.1 画面を開く
- ホーム画面で `仕訳入力` を押します。
- または仕訳帳、出納帳、総勘定元帳、取引先別取引一覧から `新規仕訳` や `編集` を押します。

### 7.2 入力項目
ヘッダ:
- 取引日
- 伝票番号
- 証憑番号

定型仕訳:
- 定型仕訳選択
- 定型仕訳名
- `読込`
- `保存`
- `削除`

証憑ファイル:
- `証憑ファイルを追加`
- 添付済みファイルの `開く`
- 添付済みファイルの `外す`

明細:
- 借方コード
- 借方科目
- 貸方コード
- 貸方科目
- 借方税区分
- 借方入力方式
- 借方金額
- 貸方税区分
- 貸方入力方式
- 貸方金額
- 摘要

補足情報:
- 取引先
- 請求書番号
- インボイス情報表示

### 7.3 通常モードで登録する
1. `単仕訳モード` をオフにします。
2. 必要な明細を入力します。
3. 必要に応じて `証憑ファイルを追加` から領収書や請求書を添付します。
4. 借方合計と貸方合計が一致していることを確認します。
5. `登録` を押します。

注意:
- 合計不一致では登録できません。
- 金額のみ入力して科目未選択の行はエラーになります。
- 証憑ファイルは伝票単位で保存されます。

### 7.4 単仕訳モードで登録する
1. `単仕訳モード` をオンにします。
2. 各行に 1 取引ずつ入力します。
3. 借方金額を入力すると貸方金額に同額が自動反映されます。
4. `登録` を押します。

結果:
- 入力した各行が別伝票として保存されます。
- 連番の伝票番号が自動採番されます。

注意:
- 既存伝票の編集では単仕訳モードは使えません。
- 単仕訳モードでは証憑ファイルを添付できません。

### 7.5 補助科目が未指定の場合
補助科目が必要な行で未指定のまま登録すると確認ダイアログが表示されます。

選択肢:
- `0 で登録する`
- `キャンセル`

`0 で登録する` を選ぶと、既定補助科目として登録されます。

### 7.6 仕訳を削除する
1. 既存伝票を編集状態で開きます。
2. `削除` を押します。
3. 確認ダイアログで `削除する` を押します。

削除結果:
- その伝票内の全仕訳行が削除されます。
- その伝票に添付した証憑ファイルも削除されます。
- 残高が再計算されます。

### 7.7 定型仕訳を使う
よく使う仕訳は定型仕訳として保存できます。

保存する:
1. 仕訳入力画面で明細を入力します。
2. `定型仕訳名` を入力します。
3. `保存` を押します。

読み込む:
1. 定型仕訳の一覧から使いたい定型を選びます。
2. `読込` を押します。
3. 日付、伝票番号、金額、摘要などを確認して登録します。

削除する:
1. 定型仕訳の一覧から削除する定型を選びます。
2. `削除` を押します。
3. 確認ダイアログで削除します。

### 7.8 証憑ファイルを添付する
1. 通常モードで仕訳を入力します。
2. `証憑ファイルを追加` を押します。
3. PDF や画像ファイルを選択します。複数選択できます。
4. 添付一覧にファイル名とサイズが表示されます。
5. `登録` を押します。

添付後の操作:
- `開く`
  - 添付ファイルを一時ファイルとして開きます。
- `外す`
  - 添付一覧から外します。登録済み伝票を保存すると反映されます。

注意:
- 仕訳 CSV には証憑ファイルは含まれません。
- 単仕訳モードで登録したい場合は、添付ファイルを外してから登録してください。

## 8. 仕訳帳
### 8.1 表示方法
1. ホーム画面で `仕訳帳` を押します。
2. `月次` または `年度` を選びます。
3. `前へ` `次へ` `当期` で対象期間を切り替えます。

表示内容:
- 対象期間の仕訳一覧
- 借方合計
- 貸方合計

### 8.2 できる操作
- `新規仕訳`
- `編集`
- `削除`
- `CSV取込`
- `CSV出力`
- `PDF出力`
- `検索機能を表示`

### 8.3 仕訳を検索する
1. `検索機能を表示` を押します。
2. 必要な条件を入力します。
   - 取引先
   - 勘定科目
   - 金額下限
   - 金額上限
   - 証憑番号
   - 摘要キーワード
3. `検索` を押します。

補助操作:
- `条件クリア`
  - すべての検索条件を空にして再表示します。
- `検索機能を閉じる`
  - 検索欄を閉じます。条件が残っている場合はボタンに条件数が表示されます。

注意:
- 検索は伝票単位で判定されます。
- CSV出力とPDF出力は、現在の検索結果を対象にします。
- 金額下限は金額上限以下で入力してください。

### 8.4 仕訳 CSV を出力する
1. 仕訳帳画面で対象期間を表示します。
2. 必要に応じて検索条件を指定します。
3. `CSV出力` を押します。
4. 保存先を指定します。

### 8.5 仕訳 CSV を取り込む
1. 仕訳帳画面で `月次` 表示にします。
2. 取り込み対象の月を表示します。
3. `CSV取込` を押します。
4. アプリ出力形式の CSV を選択します。
5. 取込後、対象月の一覧を再確認します。

注意:
- ヘッダが一致しない CSV は取り込めません。
- 仕訳日が対象月と合わないデータは事前に確認してください。
- 年度表示では CSV 取込はできません。
- ロック済み月次には取り込めません。
- 証憑ファイルは CSV 取込対象外です。

### 8.6 PDF を出力する
1. 必要に応じて検索条件を指定します。
2. `PDF出力` を押します。
3. 保存先を指定します。
4. 保存後、自動で PDF が開きます。

## 9. 出納帳
1. ホーム画面で `出納帳` を押します。
2. 勘定科目を選択します。
3. 必要に応じて補助科目を選択します。
4. `前月` `次月` `当月` または `再表示` で内容を確認します。

表示内容:
- 前月残高
- 入金合計
- 出金合計
- 残高

`変更` から該当仕訳を編集できます。

## 10. 総勘定元帳
1. ホーム画面で `総勘定元帳` を押します。
2. 勘定科目を選択します。
3. 必要に応じて補助科目を選択します。
4. `再表示` で更新します。

表示内容:
- 繰越残高
- 借方合計
- 貸方合計
- 残高

操作:
- `編集`
- `仕訳を新規作成`
- `PDF出力`

## 11. 取引先別取引一覧
1. ホーム画面で `取引先別取引一覧` を押します。
2. 取引先を選択します。
3. 期間を指定します。
4. `再表示` を押します。

表示内容:
- 該当取引一覧
- 借方合計
- 貸方合計

`編集` から元仕訳を修正できます。

## 12. 試算表
1. ホーム画面で `試算表` を押します。
2. 集計開始日と終了日を指定します。
3. `再表示` を押します。

表示内容:
- 前月残
- 借方
- 貸方
- 残高
- 分類別集計

## 13. 貸借対照表
1. ホーム画面で `貸借対照表` を押します。
2. 基準日を指定します。
3. `再表示` を押します。

表示内容:
- 資産の部
- 負債の部
- 資本の部
- 当期純損益

操作:
- `PDF出力`

## 14. 損益計算書
1. ホーム画面で `損益計算書` を押します。
2. 期間開始日と終了日を指定します。
3. `再表示` を押します。

表示内容:
- 売上高
- 売上原価
- 売上総利益
- 営業利益
- 販管費
- 営業外等収益
- 営業外等費用
- 当期純損益

操作:
- `PDF出力`

## 15. 消費税集計表
1. ホーム画面で `消費税集計表` を押します。
2. 開始日と終了日を指定します。
3. `再表示` を押します。

表示内容:
- 税区分別の課税対象額
- 消費税額
- 控除可税額
- 控除不可税額

## 16. 会社管理、月次ロック、年度締め
### 16.1 会社情報を変更する
1. ホーム画面で `会社管理` を押します。
2. 次の内容を編集します。
   - 会社名
   - 会計年度開始日
   - 締め日
   - 消費税記帳方式
   - `免税事業者`
3. `会社情報を保存` を押します。

### 16.2 月次をロックする
1. `会社管理` 画面の `月次ロック` セクションを確認します。
2. `対象日` にロックしたい月に属する日付を指定します。
3. 対象期間とロック状態を確認します。
4. `月次をロック` を押します。

実行結果:
- 対象月の仕訳登録、更新、削除、CSV取込ができなくなります。
- 操作履歴に記録されます。

### 16.3 月次ロックを解除する
1. `対象日` に解除したい月に属する日付を指定します。
2. `解除理由` を入力します。
3. `月次ロックを解除` を押します。

注意:
- 理由未入力では解除できません。
- 解除理由は操作履歴に残ります。

### 16.4 年度締めを実行する
1. `会社管理` 画面の `年度締め` セクションを確認します。
2. 対象期間、繰越先の資本科目、当期純利益、締め状態を確認します。
3. `年度締めを実行` を押します。

実行結果:
- 翌年度開始日に繰越仕訳が作成されます。
- 対象年度が締め済みになります。
- 締め済み年度の仕訳は修正できなくなります。

### 16.5 年度締めを解除する
1. `締め解除理由` を入力します。
2. `年度締めを解除` を押します。

注意:
- 理由未入力では解除できません。
- 解除後は必要な仕訳修正を行い、再度 `年度締めを実行` してください。

### 16.6 DBバックアップを保存する
1. `会社管理` 画面の `DBバックアップ` セクションを確認します。
2. 現在のDBファイルを確認します。
3. `DBバックアップを保存` を押します。
4. 保存先とファイル名を指定します。

注意:
- バックアップは現在利用中の SQLite DB ファイルをコピーします。
- 重要な操作の前や定期的な退避に利用してください。

## 17. 操作履歴
管理者のみ利用できます。

1. ホーム画面で `操作履歴` を押します。
2. 操作日時、ユーザー、操作種別、対象、詳細を確認します。

主な記録対象:
- 仕訳作成、更新、削除、CSV取込
- 定型仕訳の保存、削除
- 年度締め、年度締め解除
- 月次ロック、月次ロック解除

## 18. 消費税入力の考え方
### 18.1 税入力方式
会社設定によって選択肢が変わります。

- 税込方式 (`gross`)
  - `税計算なし`
  - `税込`
- 税抜方式 (`net`)
  - `税計算なし`
  - `税抜`
- 免税事業者
  - 税計算は行われません

### 18.2 インボイス情報
仕入系税区分を選択すると、行ごとに次を確認できます。
- インボイス区分
- 登録番号
- 控除率
- 消費税額
- 控除可税額
- 控除不可税額

## 19. CSV 利用上の注意
- 取込前に、まずアプリから `CSV保存` / `CSV出力` を行い、その形式に合わせて編集してください。
- 任意ヘッダの CSV は取り込めません。
- 文字コードは UTF-8 を推奨します。
- 数値列は文字や記号を混ぜず、数値のみを入力してください。
- 仕訳 CSV には証憑ファイルは含まれません。

## 20. PDF 出力時の注意
- 出力するデータが未表示のときは PDF を出力できません。
- 保存後、通常は自動で PDF が開きます。
- 自動表示されない場合でも、保存先フォルダに PDF があるか確認してください。
- 仕訳帳で検索条件を指定している場合は、検索結果が PDF 出力対象になります。

## 21. ログアウトと終了
- ホーム画面右上の `ログアウト` を押します。
- ウィンドウの閉じる操作を行うと終了確認が表示されます。
- 終了する場合は `終了する`、戻る場合は `キャンセル` を押します。

## 22. ALL CLEAR
管理者のみ利用できます。

手順:
1. ホーム画面で `会社管理` を押します。
2. `ALL CLEAR` セクションの `ALL CLEAR` を押します。
3. 確認ダイアログで `CLEAR` と入力します。
4. `実行する` を押します。

注意:
- ユーザー、勘定科目、補助科目、取引先、仕訳をまとめて削除する操作です。
- 実行後はログイン画面へ戻ります。

## 23. よくある確認ポイント
- DB に接続できない:
  - DB ファイルの保存先、存在有無、アクセス権を確認してください。
- ログインできない:
  - ログイン ID、パスワード、有効フラグを確認してください。
- 仕訳登録できない:
  - 借方貸方の合計一致、金額入力、科目選択、締め済み年度、ロック済み月次かどうかを確認してください。
- CSV 取込できない:
  - ヘッダ名、列数、数値形式、表示単位が月次か、対象月がロック済みでないかを確認してください。
- 年度締めを解除できない:
  - 締め解除理由を入力しているか確認してください。
- 月次ロックを解除できない:
  - 解除理由を入力しているか確認してください。
- 証憑ファイルを添付できない:
  - 単仕訳モードがオフになっているか確認してください。
